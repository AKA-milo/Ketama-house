document.addEventListener("DOMContentLoaded", function () {
  const typed = new Typed(".typed-text", {
    strings: ["Estudio de Grabación", "Producción Musical", "Masterización", "Beats Instrumentales"],
    typeSpeed: 60,
    backSpeed: 40,
    backDelay: 1000,
    loop: true,
    smartBackspace: true,
  });
});
